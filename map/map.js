// Script pour la carte interactive (mock)
document.addEventListener('DOMContentLoaded', () => {
  // Ici tu peux intégrer une carte interactive (Leaflet, Mapbox, etc.)
  // Pour l'instant, c'est un placeholder.
});
